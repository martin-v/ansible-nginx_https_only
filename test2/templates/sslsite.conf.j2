server {
        listen 443 ssl default_server;
        listen [::]:443 ssl default_server;

        server_name {{ nginx_https_server_name }};

        ssl_certificate /etc/nginx/ssl/httptest.martinvietz.de/fullchain.pem;
        ssl_certificate_key /etc/nginx/ssl/httptest.martinvietz.de/privkey.pem;
        ssl_trusted_certificate /etc/nginx/ssl/httptest.martinvietz.de/fullchain.pem;

        root /var/www/html;

        location / {
                try_files $uri $uri/ =404;
        }
}
